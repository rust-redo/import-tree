import React from 'react'
import {read} from 'fs'
import * as Path from 'path'
import os, {type CpuInfo} from 'os'
import type {ReadPosition} from 'fs'
import {parse} from 'semver'

export default () => {
  return <div />
}